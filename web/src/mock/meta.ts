import { TMeta } from "@/types/meta";

const initialMeta: TMeta = {
  activePage: 1,
  take: 10,
  itemCount: 0,
  pageCount: 0,
  hasPreviousPage: false,
  hasNextPage: false,
};

export default initialMeta;
